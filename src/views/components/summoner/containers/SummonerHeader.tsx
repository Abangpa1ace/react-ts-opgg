
import React from 'react'
import styled from 'styled-components'

type Props = {
  summonerInfo: SummonerDto;
}

const SummonerHeader: React.FC<Props> = ({ summonerInfo }) => {
  return (
    <ScSummonerHeader>
      
    </ScSummonerHeader>
  )
}

const ScSummonerHeader = styled.div`
  
`

export default SummonerHeader